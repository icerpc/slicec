name: "Check spelling"

on:
  pull_request:
  push:

permissions:
  contents: read

jobs:
  spellcheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: streetsidesoftware/cspell-action@v8
        with:
          files: |
            **/*.{rs,md,json}
